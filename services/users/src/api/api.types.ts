export enum AuditDataChangeType {
  created = 'CREATED',
  updated = 'UPDATED',
  deleted = 'DELETED'
}

export enum AuditAssignmentType {
  added = 'ADDED',
  updated = 'UPDATED',
  removed = 'REMOVED'
}
